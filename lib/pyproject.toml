
[build-system]
requires = ["flit_core>=3.4"]
build-backend = "flit_core.buildapi"

[project]
name = "ml_hep_off_an_lib"
version = "0.1.0"
description = "Library for machine learning high energy physics project."
